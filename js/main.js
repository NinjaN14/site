/*function escrevendo(){
    const textoArray = elemento.innerHTML.split('');
    elemento.innerHTML = '';
    textArray.forEach((letra, i) =>{
        setTimeout(() => elemento.innerHTML += letra, 75*i);
    });
}

const titulo = document.getElementById('escrever');
escrevendo(titulo);*/

function database(){
    alert('Esta compra requer negociação.')
}
